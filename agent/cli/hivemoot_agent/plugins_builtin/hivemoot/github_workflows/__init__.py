"""Hivemoot GitHub-workflow subsystem.

Autonomous contribution operating mode + buzz role loading.  Shares
a prompt block and the top-level skill bundle with the rest of the
``hivemoot`` plugin.  Co-loads with the ``github`` plugin — reads
its typed config for ``repos[0]`` and ``workspace``.
"""
